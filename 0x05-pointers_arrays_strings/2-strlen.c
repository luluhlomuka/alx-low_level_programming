#include "main.h"
/**
 * main - The length of a string
 * @str: get the length of the string
 * Return: The lenght of a string
 */
int _strlen(char *s);
{
	size_t length =0;
	while (*str++)
		length++;
	return (length);
}
